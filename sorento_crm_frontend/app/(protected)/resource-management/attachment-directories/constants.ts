/** Used when viewing all deleted attachments (trash). */
export const TRASH_VIEW_ID = '__trash__';

/** Prefix for trash folder selection, e.g. "trash:uuid". */
export const TRASH_FOLDER_PREFIX = 'trash:';

/** "All attachments" view: do not filter by directory so uploads without a folder (e.g. stock list) appear. */
export const FOLDER_ALL_ID = 'folder-all';
