import { ListPageSkeleton } from '@/components/common/ListPageSkeleton';

/** Held while this list's chunk and first page arrive (M5-01 run 3). */
export default function Loading() {
  return <ListPageSkeleton />;
}
