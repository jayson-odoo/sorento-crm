# TCK-2026-000034 — Deferred director directives (backlog, NOT current priority)

> **Source:** directors' session 2026-06-17. Captured so nothing is lost. The user explicitly **deprioritised** these for now — current priority is TCK-28..33 (form SLA escalation, WhatsApp escalate+ack, conversation SLA summary channels, per-user phone/link/prefs, KPI dashboard, PWA).
> **Priority:** backlog · **Category:** epic/discovery
> **Status:** parked

Each item below is a future epic to be sized later, not a ready-to-pick spec.

## A. Feature voting (ref: EcoHub AI)

When a user asks for something the AI can't do, or requests an enhancement, the assistant **suggests logging it as a feature request** with a generalised description. Future similar requests → AI asks "is this what you mean?" to **upvote** the existing entry. Management dashboard shows results (initiator, upvoters, counts); manual upvote also supported.

- Needs: `feature_requests` + `feature_request_votes` tables; an AI-assistant tool to create/match/upvote (RAG over existing requests to dedupe — mirror the embedding pipeline); a management dashboard; manual upvote UI.
- Open question: matching threshold for "is this what you mean?" (embedding similarity cutoff).

## B. KPI-driven tasks & performance tied to Sorento

Bind staff KPIs/performance to system activity (builds on the SLA KPI store in TCK-2026-000032).

- Needs: KPI definitions per role, targets, scoring, periodic roll-up. Discovery needed on what metrics management wants graded.

## C. On-field users → Meta WhatsApp via Respond

All on-field users with company phone numbers added to Meta WhatsApp for full control via Respond.io.

- Operational onboarding (Meta Business + Respond.io contact provisioning) + the user↔RespondContact link from TCK-2026-000031 reused here. Largely process, partly the link plumbing.

## D. HR administrative module (claims, leave, etc.)

Interview HR department about admin tasks (claims, leave). **Deferred** — user said HR interview is not a current priority.

- Needs: requirements interview first, then module design (likely a new domain under `app/api/v1/` + FE pages).

## Notes

- Escalation-to-management-via-WhatsApp (CK / Jayden / Mr. Loo) from the same session is **already covered** by TCK-2026-000029 — not parked.
- Per-user phone capture + RespondContact link from the same session is **covered** by TCK-2026-000031.
