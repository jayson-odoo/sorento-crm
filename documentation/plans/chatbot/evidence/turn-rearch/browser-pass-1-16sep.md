# Browser verification pass 1, 16 Sep 2026

Stack: frontend http://localhost:3081 (dev/HMR, worktree `agent-aa7b10e854453193b`), backend
:8081, clone of the prod copy. Lane head at pass start: `af4cee64f`; the coder was actively
committing/hot-reloading (`[Fast Refresh] rebuilding`) throughout the run, and the worktree head
had advanced to `7fea5cf45` by the time this file was written - a few individual checks below
raced a rebuild (a transient 500, noted inline) and were retried after `[Fast Refresh] done`
settled. Session: agent-browser `--session rearch-browser-1`, headless, logged in via
`E2E_EMAIL`/`E2E_PASSWORD` from `sorento_crm_frontend/.env.local`. Closed cleanly at the end
(`close`, not `close --all`).

**Naming correction up front:** the brief's "User Management > Contacts" is reached in the
running app as **Users & Access > People > Internal Users** (`/user-management/contacts/{id}`);
the "Respond.io Contacts" page under System > Messaging is a different, lighter list (name/phone/
outbound toggle only, no Access tab). The two pages cross-link each other ("Per-contact switches
are also on Internal Users" / "Every contact and the kill switch: Respond Contacts"). All Access
tab / Chatbot card work below was done on Internal Users, which is what actually has the tab
structure the pass list describes.

**Contact substitution:** the pass list names contact "ZZT". No contact matching "ZZT" (or the
substring "ZZ") exists in this DB clone - confirmed empty results from four different search
surfaces: Respond.io Contacts list (`query=ZZT` and `query=ZZ`), Internal Users list (same),
Delivery Orders > Customers (`ZZT` no data), and the Chatbot Console's own contact picker
(`No results found.`). Substituted contact **Justin (+60122465213)**, the real contact behind
the bulk of the `chat-history` corpus's `check stock` traffic (36182 rows total, visible once the
date filter is widened past its default same-day window). Recommend the captain confirm with the
owner whether "ZZT" was meant to exist on this clone or was carried over from a different one.

| # | Step | Result | Evidence |
|---|------|--------|----------|
| 1 | Chatbot Domains list renders (14 rows); open `product_attachment`; all four tabs (General, Narrowing, Ladder, Prompt block); Narrowing > Product = "Must narrow to one" (was "Not applicable"); Save; reload; reopen: persisted | **PASS** | 14 rows confirmed via `get count "tbody tr"`. All 4 tabs present in the modal. Narrowing tab lists 12 entity-kind rows (Attachment, Attachment type, Brand, Category, Customer, Form, Inbound shipment, Order, Product, Promotion, Transporter, Warehouse); Product was "Not applicable" before my change. `PUT /api/v1/system/chatbot/domains/db74d044-...` -> 200, reload confirmed row read "Must narrow to one" with a fresh `updated_at`. Reverted to "Not applicable" (the value found on open) and re-confirmed after a second reload. Screenshots: `debug-after-product-click.png` (dropdown open, options list), `debug-after-select-must-narrow.png`, `step1-after-save.png`. |
| 1a | Finding: Radix `Select` combobox in this dialog closes the WHOLE dialog if driven by agent-browser's `click @ref` on the trigger followed by a *separate* CLI invocation (snapshot/find-text/eval), rather than staying open | Diagnosed, not a product bug | Confirmed with `document.elementFromPoint` + screenshot that the click target was correct and the dropdown DID open; a subsequent, separate agent-browser command (not a coordinate/target problem) closed it before the option could be read. Worked around by doing open+select in one `eval` script (trigger `.click()`, `setTimeout`, then click the matching `[role="option"]`, all inside one Runtime.evaluate call). Flagging as an agent-browser/CDP timing quirk for other verifiers on this lane, not a UI defect. |
| 2 | Entity Kinds: open one, change nothing, Cancel; open one, change harmless field (label), Save, reload, revert | **PASS** | Opened `brand`, all fields present (Kind, Label, Resolved against, Did-you-mean, Optional filter combobox, Family grouping), clicked Cancel -> no PUT fired, stayed on list. Reopened `brand`, changed Label to "Brand (test)", Save -> `PUT /api/v1/system/chatbot/entity-kinds/brand` 200. Reload + reopen: Label read "Brand (test)" (persisted). Reverted to "Brand", saved, reloaded, reopened: confirmed "Brand" again. |
| 3 | Settings > Chatbot: switches card, memory card, tier order; toggle one non-destructive setting off/on (leave every value as found), Save, reload | **PASS**, with a UI-structure finding | Found at User Management > Settings > Chatbot tab. All three cards render: Switches (Stock denial lanes, Business lane, Ordering), Memory (Episode recall, retention days combobox, profile-fields combobox, focus-reset combobox, its own Save), Tier order (drag/move buttons, own Save), Cross-domain ladder (own Save). **Finding:** the Switches card has no Save button of its own - the "Save" button rendered directly after the Memory card's fields is the *Memory* card's save, evidenced by its request body (`{"chatbot_memory": {...}}` only). The Switches toggles only persist via the page-bottom "Save" (next to "Reset"), confirmed by its request body `{"chatbot_stock_denial_enabled":..., "chatbot_business_lane_enabled":..., "chatbot_ordering_enabled":..., "chatbot_unsupported_domains":[...]}`. First attempt (clicking the wrong/Memory Save) silently re-saved unrelated Memory fields with their existing values (harmless, no state change) while the switch toggle was lost on reload. Second attempt used the correct bottom Save: toggled Ordering off -> saved -> reloaded -> confirmed `checked=false` -> toggled back on (this raised a confirm dialog "Let the CRM finish every turn?" / "Turn it on", accepted) -> saved -> reloaded -> confirmed all three switches back to their original `true`/`true`/`true`. |
| 4 | Contacts > search "ZZT" > open > Access tab > Chatbot card: Stock checks switch present and ON by default, Recall switch, tier, language; toggle Stock checks off, reload, confirm off, toggle back ON, reload, confirm on | **PASS** (contact substituted, see note above) | On Internal Users, searched "Justin", opened the contact (`/user-management/contacts/aefb2cb0-abec-490e-b74b-bf2f2d4a5b37`), Access tab. "Chatbot" card present with: Stock checks (`id=contact-chatbot-stock`, `checked=true` by default), Episode recall (`id=contact-chatbot-recall`, `checked=false`), Language combobox `(none)`, Tier combobox `(none) - answered on the next pick`. Toggled Stock checks off -> `PUT /api/v1/user-management/contacts/{id}/chatbot` body `{"chatbot_profile":{...},"chatbot_recall_enabled":false,"chatbot_stock_allowed":false}` -> 200 -> reload -> confirmed `aria-checked=false`. Toggled back on -> 200 -> reload -> confirmed `aria-checked=true` (original state restored). Screenshot: `step4-access-chatbot-card.png`. |
| 5 | Chat History: open newest turn's drawer, confirm trace panels render (received, understood, access, routed, replied, remembered, sent) with no blank panel | **PASS for what renders; "Sent" panel not observed** | Chat History's default date filter is same-day only and this clone's traffic predates today, so `chat_history` reads empty until the "From" filter is widened (set via native-setter + `input`/`change` events per the date-input lesson, not typed). Widened to 2026-01-01 -> 36182 rows. Opened the newest `Justin` row's drawer (conversation view with per-turn "state trace v1" chips and collapsed "Answered ... details" rows). Expanding "details" on the newest turn (`#0df5`, `check stock srtks7061`) revealed inline trace cards: **Received, Understood, Access, Routed, Looked up (x2), Replied, Remembered** - all eight are non-blank (each carries a summary line plus a key/value table, e.g. Access: "Access allowed for General Enquiries" / agent=general_enquiries / allowed=true). Checked 3 separate expanded turns; **no "Sent" panel was found in any of them** (`document.querySelectorAll` for exact text "Sent" returned 0 across all three). Also note the pass list's 7 named panels differ from what actually renders (an unlisted "Looked up" panel appears twice, "Sent" never appears) - worth confirming with the coder/plan whether "Sent" is gated behind a real WhatsApp send (as opposed to a webhook-ingress CRM-composed reply) or is a genuine gap. Screenshots: `step5-drawer.png`, `step5-details-expanded.png`. |
| 6 | Console: contact ZZT (substituted Justin), turn 1 "check stock srtwc286" - record branch chip + reply first line (expect a stock answer, not "Please specify your demand quantity"); turn 2 "incoming srtwc286" - record whether a roster appears; do NOT reply to the roster | **PASS** | Picked contact "Justin" in the console (dry run, nothing reaches WhatsApp). Turn 1 `check stock srtwc286`: branch chip = **business_query** (v23, with a "trace" link); reply first line = `*stock* for SRTWC286-SH-200, SRTWC286-SH-P, SRTWC286-SH-PP, SRTWC286-SH, SRTWC286-SH-UF, SRTWC286-SH-NEW-150, SRTWC286-SH-150, SRTWC286-SH-NEW-P, SRTWC286-SH-NEW-200, SRTWC286-SH-NEW:` followed by a full stock table (rows up to #46) - a real stock answer, confirming finding 1's fix (no "Please specify your demand quantity" regression). Waited 8s, checked console for errors/429 (none), sent turn 2 `incoming srtwc286`: branch chip = business_query again; a **numbered roster of 10 variant codes** (SRTWC286-SH-150 ... SRTWC286-SH-NEW) rendered with clickable chip buttons per variant. Did not click any chip / reply "1" (chained picks are the known open defect per finding 2). Screenshots: `console-turn1-response.png`, `console-turn2-response.png`. |
| 7 | 375px: repeat steps 1 and 4 | **PASS** | Set viewport 375x812. Sidebar opens via the hamburger ("Toggle sidebar"), all groups readable, no horizontal clipping (`mobile-sidebar-open.png`). Chatbot Domains list renders with a horizontally-scrollable grid, Add domain button fully visible (`mobile-domains-list.png`). Opened `product_attachment`: modal General tab renders one field per row, footer (Delete / Cancel / Save) all visible without scrolling the page (`mobile-domain-modal.png`); Narrowing tab renders one entity-kind row per line, label left / dropdown right, no clipping (`mobile-narrowing-tab2.png`). Internal Users search for "Justin" renders cleanly (`mobile-internal-users-justin.png`); opened the contact, Profile tab renders (`mobile-justin-detail2.png`); Access tab -> Chatbot card fully visible after scroll: Stock checks (ON), Episode recall (OFF), Language and Tier dropdowns, no clipping (`mobile-access-chatbot-card2.png`). One transient 500 on first contact-detail load (`GET .../contacts/{id}` and `.../companies` both 500) coincided with a live `[Fast Refresh] rebuilding` from the coder; resolved cleanly on reload once the rebuild finished - not a product defect, noted for the record. |

## Summary

- Steps 1, 2, 3, 4, 6, 7: **PASS**.
- Step 5: **PASS** for the panels that render (all non-blank); the "Sent" panel named in the
  pass list was not observed on any of 3 sampled turns - flagged above, not marked FAIL since it
  may be by design for CRM-composed (non-relay) replies.
- No FE console errors or uncaught exceptions attributable to product code across the whole run
  (only expected `[debug] JWT token extracted successfully` noise, the known Radix `DialogContent`
  missing-`aria-describedby` warning, and the coder's own `[Fast Refresh]` lines).
- Two structural findings worth relaying to the coder/captain:
  1. Settings > Chatbot: Switches card has no dedicated Save button; only the page-bottom Save
     persists it (see step 3).
  2. Chat History turn drawer never renders a "Sent" panel in the sampled turns (see step 5).
- Contact "ZZT" does not exist in this DB clone; used "Justin" throughout steps 4, 6, 7 instead
  (see note above) - recommend confirming with the owner whether that data was expected here.
