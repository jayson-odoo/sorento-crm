# Owner hand pass 1 (AC-1593), 16 Sep 2026, stack :3081/:8081 on 4b8f52c12, clone `sorento_ai_automation_rearch`

Each finding names the clone `chatbot.turns` id it was measured on, the cause, and the ruling.
Replay cases are recorded from these ids BEFORE the fix (AC-1593).

| # | Message | Got | Turn id | Cause (measured) | Ruling |
|---|---------|-----|---------|------------------|--------|
| 1 | `check stock srtwc286` | `demand_qty` "Please specify your demand quantity" | 7ab515a3-6d3c-4abe-9010-bfd530528b9c | Console borrowed the newest stored envelope, a smoke turn with `custom_fields: []`, so `is_allowed_stock` read as missing; switch TRUE on the clone. | Stock allowance = `respond_contacts.chatbot_stock_allowed`, default TRUE; respond.io field ignored; toggle on the Contact > Access > Chatbot card. Console borrows only `ingress=webhook, is_test=false` envelopes. |
| 2 | `incoming srtwc286` -> roster -> `1` | `access_denied` "not allowed to access general_enquiries" | dfa0c459-59f9-428f-816d-4e4f5e053755 then 313c052d-d75e-4792-afb5-d6f718d98c76 | (a) The `1` turn's envelope carried `previous_conversation_state: {}`: the clarify turn's console result had no `session_patch`, so the console UI kept its empty state and the pending was lost. (b) The pick then parsed as `casual` with `routing.suggested_agent` null and `engine` checks access on the RAW verdict routing, so `check_access(agent_code=None)` -> `deny_unknown_agent`. The default agent lives only in `lane_parse_output`, after access. | (a) Every dry-run turn that asks a question hands its `session_patch` back so the console chains. (b) The routing default is applied ONCE, before access; contract 58 fails closed on a contact not granted an agent, never on a turn that named none. Same defect would hit prod on any webhook turn the parser leaves unrouted. |
| 3 | `delivery to hanlim` | "Which customer do you mean?" listing customer CODES, two codes twice | 205c518c-0397-4c23-adb0-a98c4cfd107c | `turn/narrow.py::_options` labels an option `raw or canonical_code`; the resolver's customer rows carry the code. Six rows for four codes = distinct uuids sharing a code (coder to report why). | Customer roster labels = customer NAME (code is not what a dealer recognises). Duplicate-code rows: one line per name; the coder reports the source of the duplicates before folding. |
| 4 | `photo srtwc286` | "product attachments for photo: No matching results found. Would you like me to escalate?" | 06eb17f2-2e83-4cfb-b640-db3082563606 | `chatbot_domains.product_attachment.narrowing` = `{attachment_type: narrow_by_type}` only, so the unresolved product token was dropped and the tool ran on `photo` alone. | `product_attachment` narrowing gains `product: must_narrow_one` (the SRTWC286 variant roster, as `incoming` shows it), then the photos of the picked product. Seeded by migration (policy rows are frozen seed data), visible on the Domains page. |

Open at the time of writing: findings 2-4 not yet fixed; owner continues single-turn probes on :3081, chained picks wait for finding 2.
