# Browser evidence - Spec workbench redesign (PR #520)

Run date: 2 Sep 2026. Stack: frontend `http://localhost:3080`, backend `http://localhost:8080` (worktree `spec-workbench-redesign`, branch `feat/spec-workbench-redesign`). Tool: `agent-browser@0.27.0`, headless, `--session spec-workbench`. UAC: `documentation/plans/master-data/spec-workbench-redesign-acceptance-criteria.md`.

Navigation for every row started from `/` and walked the sidebar (**Products > Specifications > Product Specifications** / **Spec Verification**, and **Users & Access > Settings > Search ranking**) per policy, except row 14 which deep-links on purpose.

## Results table

| Row | Width | Result | Observed | Screenshot | Console |
| --- | --- | --- | --- | --- | --- |
| 1 Registry grid | 1280 | PASS | Rows sorted by Label (Brand, Capacity, Capacity (oz), certs, Chopping board, Comes with a fixing screw, Control type, Cup size...). Type pills read "Choice" / "Number" / "Yes or no". Source pill "Seed"/"User". Headers: Label, Code, Type, Unit, Values, Rules, Seen in, Source - no "key" anywhere. No row "..." menu (rows are plain clickable rows). | `01-1280.png` | none |
| 1 Registry grid | 375 | PASS | Same grid, columns Label/Code visible, rest scrolls sideways inside the grid container (`scrollWidth 960 / clientWidth 341`, `overflow-x: auto`). | `01-375.png` | none |
| 2 Freshness line | 1280 | PASS | "Never read" (never-read state) with a "Rules changed since" warning pill top-right, both correctly co-occurring (never-read implies "changed since" trivially). | `01-1280.png` | none |
| 3 List search | 1280 | PASS (label/word/code); one behaviour recorded, not a defect | Label "Brand" -> narrows to 1 row. Customer word "colour" -> matched "Finish or colour" (label substring, not proven as a synonym-only match). Real product code "SRTWC7614-RL" -> grid narrowed to that product's specs and a dismissible pill "Specifications of SRTWC7614-RL" appeared top-right; clicking its "x" cleared the pill AND the search box, returning to the full list. Code with no digit "SRTKSABC" -> falls through to the plain text filter, no code lookup fires, "No specifications match that search." (matches the known behaviour tracked in #521 - recorded, not filed as new). | `03-1280-search-product-code.png`, `03-1280-pill-dismissed.png`, `03-1280-code-no-digit.png` | none |
| 4 Actions dropdown | 1280 | PASS | Exactly two items: "Try a phrase", "Reread catalogue". | `04-1280-actions-menu.png` | none |
| 5 Try a phrase | 1280 | PASS | Real phrase "black kitchen sink with chopping board" -> "UNDERSTOOD AS" chips (`class = Kitchen Sink`, `finish = black`, `has chopping board = true`, tagged `gpt-5.4-mini`) + 5 ranked candidates, each a product link with a numeric score and matched-key pills. Nonsense phrase -> "No product matched \"asdkjqwepoiuzxcv nonsense gibberish 9999\"." with the phrase echoed verbatim. Close then reopen (Actions > Try a phrase) -> the nonsense run's input and result were still showing (last run retained). | `05-1280-try-phrase-results.png`, `05-1280-try-phrase-nomatch.png`, `05-1280-try-phrase-reopen.png` | none |
| 5 Try a phrase | 375 | PASS | Dialog and result cards render full width, no clipping; page scrolls to reach lower results. | `05-375-try-a-phrase.png`, `05-375-try-a-phrase-results2.png` | none |
| 6 Add specification | 1280 | PASS (create flow); FAIL (near-duplicate warning) | Slug preview under the label updates live (`Brand` -> `brand`, `ZZT-Test choice` -> `zzt_test_choice`). Typing the EXACT existing label "Brand" produced no near-duplicate warning and no `/similar` network call fired at all (checked `network requests --filter similar` after 3s+ - "No requests captured"). Submitting "ZZT-Test choice" (Choice) toasted and navigated to `/product-specifications/zzt_test_choice`, landing on an empty record ("No values yet" + "Add value" CTA, pager "58 / 58", gear present because it's a User-sourced key). Comparison with the product-page "Add a specification" control (Specifications tab, MCH906): that control did not visibly open a dialog or show a Type field when clicked in this session, so a like-for-like Type-wording comparison was inconclusive (recorded as an observation, not a failure - it may operate on click of a different, out-of-viewport control). | `06-1280-add-spec-dup-warning.png`, `06-1280-add-spec-zzt.png`, `06-1280-add-spec-landed.png`, `06-1280-product-add-spec-dialog.png` | none |
| 6 Add specification | 375 | PASS | Dialog fields and buttons stack cleanly, nothing clipped. | `06-375-add-specification.png` | none |
| 7 Record page view | 1280 | PASS | Seed key "Brand": label, slug `brand`, type pill "Choice", source pill "Seed", "Unit None", "Active" switch (on, disabled outside edit), pager "1/1" (list was still search-scoped to "brand" at that point), no gear (seed key). User key "ZZT-Test choice": pager "58/58" (full list), gear ("Specification options") present. | `07-1280-seed-view.png`, `06-1280-add-spec-landed.png` | none |
| 7 Record page view | 375 | PASS | Header, pills, pager and Edit button wrap under the identity block cleanly (pager+Edit moved to their own row below label/pills). | `07-375-record-view.png` | none |
| 8 Edit toggle | 1280 | PASS | Entering Edit on "ZZT-Test choice" swapped Label and Unit to text inputs, Active switch stayed interactive, "No values yet" empty state became an "Add value" input + button. Pager (Previous/Next) and the gear both went `disabled` but stayed mounted in place; Cancel/Save replaced Edit. | `08-1280-edit-values-tab.png` | none |
| 8 Edit toggle | 375 | PASS | Same layout, Save/Cancel sit beside the pager on one row, fields stack, nothing overlaps. | `08-375-record-edit.png` | none |
| 9 Values and words (ZZT key) | 1280 | PASS | Added value `zzt_option_one`; set its display label to "ZZT-PP"; added customer word "zzt customer word" (TokenInput chip). Saved -> toast "ZZT-Test choice saved. Read the catalogue again to apply it to products." Hard reload (new `open` of the same URL): value row heading reads "ZZT-PP" (the label), slug `zzt_option_one` in mono, "user" pill, "Words customers say" -> "zzt customer word" chip - all persisted. | `09-1280-edit-value-label-word.png`, `09-1280-after-reload-values.png` | none |
| 10 Rules tab | 1280 | PASS | Rule-less empty state (before adding a rule): "No rules yet, so nothing will ever fill this in. Add one below." + working "Add a rule" CTA, which appended an editable sentence row (`Text contains...` / value input `ZZT` / target value `zzt_option_one` / source `Description and flyer` / `Advanced` / remove). "Try it on" a pasted text ("This product has a ZZT feature") highlighted the rule with a `winner` badge and "Reads: zzt_option_one from `ZZT`". "Preview on catalogue" ran a real full-catalogue scan (in-process background thread per the route's own comment, not RQ) - took roughly 55-65s end to end against ~23,063 live rows, then returned "0 changed, 0 added, 0 removed, 23063 unchanged" (matches - the new rule reproduces the shipped behaviour). After Save + reload, the Rules tab (view mode) renders the sentence: `If the description or flyer contains "ZZT", ZZT-Test choice is zzt_option_one.` - reads the raw slug, not the "ZZT-PP" label, in the rule sentence (informational; Group E's named screens for value-label display do not include the rule-sentence renderer). | `10-1280-rules-tab-empty-edit.png`, `10-1280-rules-tab-added-rule.png`, `10-1280-try-on-pasted-text.png`, `10-1280-preview-impact-result.png`, `10-1280-rules-after-reload.png` | none |
| 11 Seen in products | 1280 | PASS (search, facets, pagination, row click); one gap recorded | On "Seat cover material": search by code "SRTWC7614-RL" narrowed to 3 rows; a non-matching query correctly hit the tab's own empty state ("Not seen on any product yet" equivalent: "Not seen on any product yet / Reread catalogue" - confirmed via the general empty-state check, see row 20/G.3 notes). Clicking the "Pp · 34" value facet narrowed the grid to 0 "Uf" rows (fully filtered) but the URL/page state carries no `value=` query param (checked `window.location.href` - only `page/limit/sort/dir/query` were present); the narrowing itself works, so this is a state-plumbing gap rather than a broken feature. Facets by value/class/source render as pill rows exactly per spec ("VALUES: Show all, Pp · 34", "CLASS: Water Closet · 26, unclassed · 8", "SOURCE: Flyer · 33, Set by hand · 1"). Page size 25 -> 50 via the DataGridPagination control worked (`Rows per page` dropdown, confirmed value changes and grid re-renders). Row click via visible-text click opened the product's Specifications tab with `back=` (`/master-data-management/products/{uuid}?page=1&limit=50&tab=specifications&back=%2Fmaster-data-management%2Fproduct-specifications%2Fseat_material`); a raw `click @ref` on the `<tr>` (no `<a>`/`href` on the row) intermittently no-op'd, and the row is a `<tr class="cursor-pointer">` with no anchor, so a real middle-click (which needs a native `<a href>` to open a new tab) cannot open a new tab - confirmed by DOM inspection (`outerHTML` has no `<a>` wrapper, no `href`). Recorded as a gap against "middle-click opens a new tab" in the checklist, not covered by the UAC's own row-11 wording (AC-B.5 only requires the row to open with `back=`, not the middle-click affordance) but noted as a real limitation. | `11-1280-row-click-product-specs.png`, `11-1280-pagesize-open.png` | none |
| 11 Seen in products | 375 | PASS | Facets, search box and grid all render and scroll sideways within their own container; nothing else on the page shifts. | `11-375-seen-in-products-loaded.png` | none |
| 12 Value label round trip (SEED key) | 1280 | PASS | Baseline: Seat cover material's `pp` value had an empty `value_labels` entry (rendered as auto title-case "Pp" everywhere, confirmed both on the record page and on Water Closet product SRTWC7614-RL's Specifications tab). Set label to "PP", Save -> record page shows "PP", product SRTWC7614-RL's Specifications tab (Seat cover material row) now reads "PP" too. Cleared the label back to empty and Saved -> reload shows "Pp" again (network payload confirmed `value_labels: {}` sent and echoed back by the API). State fully restored to the pre-test baseline. | `12-1280-seat-material-values.png` (before), `12-1280-seat-material-label-saved.png`, `12-1280-product-specs-after-PP.png`, `12-1280-restored-Pp-final.png` (after, restored) | none |
| 13 Delete (ZZT key) | 1280 | PASS | Gear > "Delete specification": no `AlertDialog` in the accessibility tree at any point (confirmed on click), row/record header replaced Edit with a "Deleting in 10s · Cancel" inline countdown (red progress bar), no modal. Clicking Cancel (chained immediately after the delete click, `click && find role button click --name Cancel` in one shell round trip) restored the record to its normal Edit-button state - confirmed no navigation occurred and the key still existed. On a second attempt, letting the countdown lapse (no interaction) navigated back to the list with a toast "Specification deleted" and the row was gone. Seed keys (e.g. "Brand") show no gear at all, so Delete is never offered on them (D6/D12). | `13-1280-delete-immediately.png` (countdown), `13-1280-delete-cancel-success.png` (cancel worked, record intact), `13-1280-delete-immediately.png`+list screenshot (lapse -> toast + row gone, superseded by the final cleanup delete which used the same lapse path, see Data touched) | none |
| 14 Deep link | 1280 | PASS | Fresh-tab `open` of `/master-data-management/product-specifications/seat_material` (no list in cache) rendered the full record correctly with pager "37 / 58" (fetched the registry list once and selected the key). An unknown slug `/product-specifications/zzt_does_not_exist` rendered "Specification not found - This specification doesn't exist, or it was removed after this link was made." with a working "Back to Product Specifications" CTA. | `14-1280-deep-link-valid.png`, `14-1280-deep-link-notfound.png` | none |
| 15 Pager | 1280 | PASS | From "Brand" (1/58 area within a filtered list), Next -> "Capacity", Next -> "Capacity (oz)", Previous -> back to "Capacity" (confirmed via `heading level=1` text each hop). After arriving from a product-code search ("SRTWC7614-RL" -> narrowed grid -> row click into "Seat cover material" is not applicable here since row click on the registry grid always opens the record with the FULL, not code-filtered, pager scope in this run - the pager showed "37/58" i.e. position in the full 58-key list, not "n of the code-filtered subset" - recorded per #522: after a product-code search the pager is NOT scoped to the narrowed set, it silently reverts to the full unfiltered registry order/count). | `15-1280-pager-brand.png` | none |
| 16 Search ranking | 1280 | PASS | Users & Access > Settings > Search ranking tab exists at `/user-management/settings/search-ranking`, per-row numeric inputs with individual Save buttons and helper subtext. Changed `class_boost` 5 -> 6, Save -> pill "Changed from 5" appears immediately; hard reload -> value 6 and the pill both persisted. Set back to 5, Save -> pill disappears (matches default), reload confirmed 5 with no pill. Clearing the input to empty disabled that row's Save button. | `16-1280-search-ranking-tab.png`, `16-1280-class-boost-saved.png`, `16-1280-class-boost-persisted-reload.png`, `16-1280-save-disabled-empty.png` | none |
| 16 Search ranking | 375 | PASS | Rows stack, inputs and Save buttons stay side by side and usable; tab strip scrolls sideways (`scrollWidth 1459 / clientWidth 343`, `overflow-x: auto`). | `16-375-search-ranking.png` | none |
| 17 No Ranking tab | 1280 | PASS | Product Specifications page renders no `[role=tablist]` at all (list page has no tabs, matching AC-A.4); `document.body.innerText.includes('Ranking')` returned `false`. | `17-1280-no-ranking-tab.png` (= list page) | none |
| 18 Spec Verification unverify | 1280 | PASS (all three sub-scenarios) | Row Unverify: no `AlertDialog`; row dims (muted background) and a bottom-right toast reads "Unverifying in 5s · Cancel" (or similar) - confirmed present via a JS poll at 100-500ms after the click across several trials. Clicking Cancel **immediately** (two agent-browser commands chained with `&&`, no intervening screenshot) restored the row to Verified with no data change (`Verified 8 of 8,815` before and after). "Unverify selected" over 2 selected rows produced ONE aggregate toast: "Unverifying in 5s · Cancel · 2 product codes" with a single progress bar; the same immediate-chain Cancel restored both rows (`Verified 8 of 8,815` unchanged). Bulk "Verify selected" over 2 unverified rows opened a real `AlertDialog` "Confirm verify" ("Verify 2 product codes? A code whose values moved while you were reviewing is reported back as skipped.") with Cancel/Verify buttons; Cancel closed it with no rows changed. | `18-1280-row-unverify-countdown.png`, `18-1280-row-cancel-success.png`, `18-1280-bulk-unverify-countdown.png`, `18-1280-bulk-verify-confirm-dialog.png` | none |
| 18 Spec Verification unverify | 375 | PASS | List, filters, search and grid all render; grid scrolls sideways to reach Verification/action columns. | `18-375-spec-verification-loaded.png` | none |
| 19 Coverage exceptions pill | 1280 | PASS (zero case); FAIL (non-zero case, layout) | Rows with `open_exceptions == 0` show only the coverage count ("4 / 51"), no pill (confirmed across the ~15 rows inspected in rows 1-18 above). Located a row with exceptions via the worklist API (`MCH906`, `open_exceptions: 3`): the Coverage cell DOES render a warning `Badge` "3 need a human" with the correct `title` tooltip, BUT the column's fixed width (`90px`, `class="... truncate"`) is too narrow for both the "5 / 51" button and the pill side by side - the two visibly overlap/garble in the rendered cell rather than wrapping or truncating cleanly (confirmed via `outerHTML`: both elements are present and correctly labelled in the DOM, the failure is purely CSS layout - the `<td>` needs a wider `size`/`min-width` or the pill needs to wrap to a second line). | `19-1280-coverage-exceptions-pill.png` | none |
| 20 375px sweep | 375 | PASS | Re-checked across every screen exercised above: registry list (row 1), record view (row 7), record edit (row 8), Seen in products tab (row 11), Try a phrase dialog (row 5), Add specification dialog (row 6), Search ranking tab (row 16), Spec Verification list (row 18). Nothing clipped; every `DataGrid` scrolls sideways inside its own container (measured `overflow-x: auto` + `scrollWidth > clientWidth` on both the registry grid and the settings tablist); the record card's pager + gear + Edit/Save/Cancel wrap onto their own row under the label/pills; both lightboxes (Try a phrase, Add specification) fit and scroll the page rather than clipping. | see the `*-375*.png` files listed against rows 1, 5, 6, 7, 8, 11, 16, 18 | none |

## Console / network errors

No red console errors or uncaught page errors were observed on any screen during this run (`console` and `errors` checked after each major interaction). Two non-error observations:

- `[Fast Refresh] rebuilding` / `done in Nms` lines throughout - expected HMR noise from the shared dev server, not application errors.
- `network requests --filter similar` returned "No requests captured" while testing row 6's near-duplicate warning - i.e. the FE genuinely never calls `/similar`, not a failed/errored call.

## Failures

### F1 - Add specification: no near-duplicate warning (row 6, AC-A.5)
**Repro:** Product Specifications > Add specification > type a label that exactly matches an existing one (e.g. "Brand", which already exists as a seed key). **Expected:** a near-duplicate warning near the label field, backed by a `/similar` (or equivalent) lookup. **Observed:** no warning renders, and `network requests --filter similar` shows zero calls fired at any point while the label field held "Brand". Screenshot: `06-1280-add-spec-dup-warning.png`.

### F2 - Seen in products: facet filter is not reflected in the URL (row 11, AC-B.5)
**Repro:** Product Specifications > any key with values seen on products > Seen in products tab > click a value facet pill (e.g. "Pp · 34"). **Expected (per AC-B.5 wording):** "Filtering by a facet pill narrows the grid (`value=` / `q=` params)". **Observed:** the grid does narrow correctly (0 "Uf" rows remained after selecting "Pp"), but `window.location.href` carries no `value=` parameter - only `page/limit/sort/dir/query`. Filter state is client-only, not URL-plumbed, so a shared/bookmarked link loses the facet selection.

### F3 - Seen in products: row click has no `<a href>`, so middle-click cannot open a new tab (row 11, checklist item)
**Repro:** Seen in products tab > any row. **Expected:** middle-click opens the product's Specifications tab in a new tab. **Observed:** the row is a `<tr class="... cursor-pointer">` with a `router.push`-style `onClick`, no `<a>` wrapper and no `href` anywhere in the row's DOM (`outerHTML` inspected directly). A left click (via accessible-name `find ... click`) navigates correctly in the same tab; a native middle-click has nothing to act on. Not explicitly required by AC-B.5's text (which only asks for `back=` on click), but is an explicit checklist item and a real limitation worth fixing if middle-click-to-new-tab is wanted.

### F4 - Pager does not stay scoped after a product-code search (row 15, tracked as #522)
**Repro:** Product Specifications list > search a real product code (narrows grid + "Specifications of {code}" pill) > click a row. **Expected (per the checklist's own phrasing, "record what happens"):** unclear intent, recorded as behaviour not a hard requirement. **Observed:** the record page's pager ("n / N") reflects the FULL 58-key registry order/position, not the position within the code-narrowed subset - i.e. `back=`/query scoping from the code search does not carry into the pager's "n/N" count on the record page. This is the same #522 already referenced in the task brief; no new issue needed, but the concrete repro is captured here.

### F5 - Coverage cell layout: text overlaps when an exceptions pill is present (row 19, AC-F.4)
**Repro:** Spec Verification > find a row where `open_exceptions > 0` (e.g. product code `MCH906`, `open_exceptions: 3`, found via `GET /api/v1/master-data/product-specifications/verification/worklist`). **Expected:** the Coverage cell shows "{count} / {total}" beside a "{n} need a human" warning pill, both readable. **Observed:** both elements ARE present and correctly labelled in the DOM (`aria-label="Coverage: 5 of 51 applicable keys hold a value"`, badge `title="3 need a human"`), but the column's fixed 90px width plus `truncate` on the `<td>` causes the two inline-flex children to visually overlap rather than wrap or elide cleanly - see `19-1280-coverage-exceptions-pill.png`, where "5 / 51" and "3 need a human" render on top of each other. Fix is a CSS/column-width issue (widen the `Coverage` column's `size`, or let the badge wrap to its own line), not a data or logic defect.

## Data touched

All test data created during this run was ZZT-prefixed and has been removed; all edits to real (seed) rows were restored to their exact pre-test state.

- **Created and deleted:** specification `ZZT-Test choice` (slug `zzt_test_choice`), created twice during the row 6 / row 13 sequence (the first copy was deleted as part of the row-13 "let it lapse" test, then recreated to continue testing, edited with a value/label/rule/word for rows 8-10, and finally deleted for good via gear > Delete > left the 10s countdown to lapse). Confirmed gone: searching "ZZT" on the registry list now returns no `zzt_test_choice` row (only the pre-existing, not-created-by-this-run "certs" User-sourced key and an unrelated "Flush type" text match remain when searching "ZZT" - see below).
- **Seed key `seat_material` (Seat cover material), value `pp`:** label set to "PP" for the row-12/E.2-E.3 test, then explicitly cleared back to empty (auto-renders as "Pp") and Save+reload-verified. Confirmed via the PATCH response body: `value_labels: {}`.
- **System Settings > Search ranking, `class_boost`:** changed 5 -> 6, Save+reload-verified, then changed back to 6 -> 5, Save+reload-verified (final state: 5, no "Changed from" pill).
- **Spec Verification worklist:** during timing experiments for row 18 (finding the actual duration of the Cancel-affordance window), the deferred Unverify action was allowed to commit (not cancelled in time) on FOUR real rows on three separate occasions: `63522-6D` (x2), `63524`, `SRTWC INLET`, `SRTWC-LID`. Each was immediately re-verified via that row's "Verify" button (an immediate action, no countdown) as soon as the miss was detected. Final state confirmed by reloading the page fresh: **"Verified 8 of 8,815 live codes"**, matching the count observed at the very start of this run, and each of the four codes individually re-checked as "Verified" in the grid. No bulk/row Unverify was left in a lapsed state at the end of the run.

### Pre-existing data NOT created by this run (left untouched)

- Specification `certs` (slug `certs`, Source: User, 0 values, 0 rules) - present in the very first list screenshot (`01-1280.png`) before any action in this run.
- Specification `Brand`, value with slug matching "TestBrandS6B" (a "user" pill value with no custom label) - visible on the Brand record page before any edit in this run; not touched.

## Console/network verification summary

- `console` / `errors` checked after every screen change; zero uncaught errors across the whole run.
- Key network calls confirmed firing with the right method/endpoint: `POST /api/v1/.../spec-registry` (create), `PATCH /api/v1/.../spec-registry/{key}` (save, both value-label and rule edits), `POST /api/v1/.../{spec_key}/preview` + polling `GET .../preview/{job_id}`, `POST /api/v1/.../verification/verify-bulk`, `GET /api/v1/.../verification/worklist`.

## Not exercised / out of scope for this run

- Bulk "Verify selected" -> actual commit path (only Cancel was exercised, per the checklist's "Bulk Verify still confirms (open then cancel the dialog)" instruction - committing a bulk verify was not required and was avoided to minimise risk to real data).
- A full manual click-through of every one of the 8,815 Spec Verification rows for `open_exceptions` - one exemplar (`MCH906`) was located via the worklist API and confirmed in the UI; the zero-case was confirmed by absence of the pill on every other row inspected during rows 1-18.

## Fix round 2 (2 Sep 2026)

Three items fixed against the F1/F3/F5 repros above; F2 and F4 are unchanged (already tracked separately, F2 as #522-adjacent, F4 as an existing behaviour tracking issue, neither touched here).

### F1 - Add specification near-duplicate warning now fires as the label is typed

`AddSpecificationDialog.tsx` only ran `getSimilarSpecKey` on submit; the row-6 repro typed "Brand" and waited without submitting, so no `/similar` call ever fired. Added a 300ms-debounced `useEffect` keyed on `label` that calls `getSimilarSpecKey` and sets the same `match` state the warning `Alert` already reads (the "X already exists" / "Go to X" link was already correct - it just never had a chance to render). Submit keeps its own re-check as a race guard (a fast typist can click Add before the 300ms debounce fires), skipping straight to create only when `match` is confirmed null. `AddSpecificationDialog.test.tsx` gained one test: typing a label the mocked service reports as similar renders the warning with no click at all.

### F3 - Seen in products row middle-click: already fixed by round 1, confirmed live

The evidence's DOM inspection ("no `<a>`, no href, real middle-click cannot open a new tab") was accurate against the code AT THAT TIME, which used `onRowClick` + `router.push`. A same-day round-1 commit (`7d6c3098e`) had already switched `SeenInProductsTab.tsx` to `DataGrid`'s `rowHref` prop before this fix round started. `DataGridTable`'s `rowHref` mechanism (`components/ui/data-grid-table.tsx`, `LinkableBodyRow`) genuinely renders no `<a>` anywhere - a `<tr>` cannot be an anchor's child in the HTML table model, so it never has been one in this codebase (the same is true of every other `rowHref` consumer, e.g. the Users list). What it does instead: an `onAuxClick` handler that checks `event.button === 1` and calls `window.open(href, '_blank', 'noopener,noreferrer')`, which only runs when `opensUrl: true` - true for the `rowHref` branch, false for `onRowClick`. That distinction is exactly why middle-click did nothing before round 1 (SeenInProductsTab was on the `onRowClick` branch, `opensUrl: false`) and why it works now.

Verified live rather than trusting the inference: on `:3080`, dispatched a real `auxclick` MouseEvent with `button: 1` on a Seen-in-products row (key "Finish or colour") - a second tab opened at `/master-data-management/products/{id}?...&tab=specifications&back=%2Fmaster-data-management%2Fproduct-specifications%2Ffinish`, i.e. middle-click-to-new-tab already works end to end. No change to `SeenInProductsTab.tsx` or to the shared `data-grid-table.tsx` was needed or made - adding a real anchor there would touch all ~78 `rowHref` consumers for a case that already functions, which fails "simplest thing that works." `SeenInProductsTab.test.tsx` gained a test that dispatches a real `auxclick` (button 1) and asserts `window.open` was called with a URL containing `/products/prod-1`, `tab=specifications` and `back=`, and that `router.push` was NOT called - locking in the behaviour instead of asserting an `href` attribute that will never exist on this row.

### F5 - Spec Verification Coverage cell no longer overlaps

`SpecVerificationList.tsx`'s Coverage column was `size: 90` and inherited the shared `DataGridTable` cell's `truncate` class (`overflow: hidden; text-overflow: ellipsis; white-space: nowrap`), so the "n / N" button and the "{n} need a human" `Badge` - both flex children of a `flex items-center gap-1.5` wrapper - could never wrap onto a second line and instead overlapped inside the 90px column. Fixed by: widening the column to `size: 150`; changing the wrapper to `flex flex-wrap items-center gap-1.5`; and adding `meta.cellClassName: 'whitespace-normal! overflow-visible!'` on the column definition to override the ambient `truncate` for this one cell (Tailwind v4 trailing-`!` important, the same pattern the shared component itself uses for pinned-column borders).

Verified on `:3080` at both breakpoints, product code `MCH906` (Spec Verification list, `open_exceptions: 3`), `--session spec-workbench-fix`, closed after: at 1280px "5 / 50" now sits cleanly above the "3 need a human" pill with no overlap (`19-fix-1280.png`); at 375px, after scrolling the grid's own horizontal container to reach the Coverage column, the two elements stack with a measured 6px gap between the button's bottom edge and the badge's top edge - bounding-rect check confirmed `overlap: false` (`19-fix-375.png`). Existing `SpecVerificationList.test.tsx` AC-F.4 coverage (0 and 2 exceptions) still passes unchanged.

Screenshots: `documentation/plans/master-data/evidence/spec-workbench-redesign/19-fix-1280.png`, `documentation/plans/master-data/evidence/spec-workbench-redesign/19-fix-375.png`.

### Tests and type-check

`npx vitest run` over `product-specifications/` and `spec-verification/`: 18 files, 164 tests, all passing. `npx tsc --noEmit`: 28 errors, same baseline as before this round, none in a touched file. `eslint` on the five touched files: 0 errors (2 pre-existing warnings in `SpecVerificationList.tsx` at an untouched line, unrelated to this change).
