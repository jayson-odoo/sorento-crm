# Overview

End-user documentation organised by department. The AI assistant is intended to source from these files when answering "what do I do?" / "how do I…" questions.

## Structure

* `**[_shared/](./_shared/)**` - flows reused by multiple departments (e.g. the generic Resource Management → Files upload flow). Department guides link here instead of repeating the same steps.
* `**[purchasing/](./purchasing/)**` - packing list upload, proforma invoice upload (a supplier's own workbook, read line by line), translating a supplier's own wording into English on that invoice, product attachments, folder management, stock-inquiry review, product-master upload, SPO upload.
* `**[warehouse/](./warehouse/)**` - GRN upload (header + lines), Delivery Order upload (tracking + lines).
* `**[marketing/](./marketing/)**` - promotion upload, marketing-form upload, catalogue packages (Combos, including a combo's own cover picture) on a product and the price tags they print, working a price tag request (design, any product on the line's combo bindable on any tag, marking a tag Not printed, automatic arrange, pinned change requests, printing and collection, product-data auto-apply with rollback, design history).
* `**[project-sales-admin/](./project-sales-admin/)**` - review submitted purchase requests / sponsorship forms and send them for approval.
* `**[project-sales-manager/](./project-sales-manager/)**` - approve purchase requests / sponsorship forms via emailed token link, flow stock inquiries to purchasing.
* `**[project-sales-rep/](./project-sales-rep/)**` - portal access (WhatsApp link + OTP), how to file complaints / stock inquiries / purchase requests / sponsorship forms / price tag requests, and what happens to a price tag request afterwards (design review, pinned change requests, approve, collect).
* `**[system-management/](./system-management/)**` - admin/ops reference: operational logs and configuration tables (imports, integration logs, scheduled tasks, email/WhatsApp delivery, import column mappings including the per-supplier Stock list words vocabulary a stock-list upload composes a product code from), troubleshooting a failed notification, reading/retrying a chatbot turn trace (including the full trace panel's Apply, Memory and Sent sections), the chatbot's photo and voice note intake (what it reads or hears, naming a code it could not find, and why a photo or voice note may be refused) and the Chat History thread's photo thumbnail / audio player, its "Read N items" chip, and its "Read the photo" / "Heard the voice note" trace stage, the chatbot's "last purchase cost" answer and the per-contact Field reveal that gates it, the chatbot's outstanding report (sales order backlog / delivery order pending) and its own Field reveal gate, the chatbot's sales report (confirmed vs outstanding sales by month, by customer or product, the sales order list on "1") and its own Field reveal gate, the chatbot's product spec visibility policy (contact override / market segment / default), the chatbot's "which products have X" counted set answers (plus the Lookup Sets an admin fills in to teach it new words), configuring Chatbot Domains (what the bot answers, narrowing, the ladder, the prompt block) and Entity kinds (what it resolves names against), and the AutoCount integration contract.
* `**[user-management/](./user-management/)**` - manage users/roles/teams, admin Settings pages including Portal Revisions (letting a dealer revise a submitted portal form instead of starting over) and Chatbot settings (switches, memory, tier order, cross-domain ladder), the per-contact Chatbot card (stock checks, recall, tier, language), Market Segments granting a contact extra portal forms beyond the base four, and the per-contact Portal forms override (Inherit / Always show / Always hide).
* `**[supply-chain/](./supply-chain/)**` - uploading the sales order book, purchase order book, Order Inquiry sheet and reorder-level listing that a reorder plan is built from; a Loading Plan stock-list upload composing our product code from a supplier's bare model number via brand / product-type / trap-size words, and the Stock list words link on the Supplier codes tab; running a reorder plan and confirming decisions; printing the order summary sheet; the low stock report workbook (Low stock / All sheets) from a plan's Actions menu or over WhatsApp, its Field reveal gate, and the Actions menu's own split dialog (None / Supplier / Category / Supplier x Category, with a rows/sheets preview) for splitting that workbook into a pair of sheets per supplier or category; deciding Buy vs Borrow on Fulfilment Planning; planning a sales order nobody decided (delivered or Completed included) and reading the Planned pill on the Sales Orders list; what happens when a sales order changes after planning (manual edit, book upload, or AutoCount) and how CS Confirms or Amends it, including the year on the What changed dialog's dates and the `used` row Confirm raises when a linked document is already fully received; the `received`, `reallocate`, `unlink` and `note` words on an Order Inquiries PO/SPO chip and what each one's lightbox shows; the Order Inquiries page opening on To confirm by default and confirming a row (tick or Select all N records, then Confirm (N)) so it feeds reorder planning, and its remembered sort and filters; the worklist's separate Customer and Project columns, each sorting on its own, and a pre-order's PRE-ORDER note in the Project cell; the info icon on Raised at listing a re-confirmed line's earlier raises, newest first; Unconfirm putting a Confirmed or Changed row back to To confirm, with no email and no effect on rows already To confirm, Rejected or cancelled; the order inquiry handover email to purchasing during the parallel run, what sends it, and how to manage its recipients via System Management, Automation; undoing the board's last Confirm from the gear menu, within the countdown, and the automatic email that tells purchasing.


## Conventions

Every guide follows the same shape:


1. One-line summary of when to use the flow.
2. **Steps** - short numbered list with exact UI labels in **bold**, taken verbatim from the React components.
3. **What's captured** / **What gets created** - the data the system records.
4. **How you'll be notified** - in-app toast vs. WhatsApp / email vs. background processing.
5. **Bulk import** (where supported).
6. **See also** - cross-links to related guides.

UI labels (menu items, page titles, button text, dialog titles) are quoted **verbatim from the frontend**, not paraphrased - if a label changes in the code, the guide must change too. We use frontend names (e.g. *Files*, *Packing Lists*, *Delivery Orders*) rather than backend / database names (e.g. *Attachments*, *Inbound Shipments*, *Order Tracking*).

## Status

* ✅ Purchasing - drafted, fact-checked against the codebase + verified live with Playwright.
* ✅ Warehouse - drafted (GRN, Delivery Orders).
* ✅ Marketing - drafted (Promotion, Marketing Form, catalogue packages on price tags: Combos and Sold with on the product, parts and the package warning on a request, an unresolved choice group auto-splitting into one tag per candidate, the combo's own cover picture, the combo tag's per-layer Product picker now offering every product of the whole combo grouped by role; Price Tag Requests: Design section + full-screen viewer, marking a tag Not printed, automatic size-grouped arrange with a Per A4 grid override, pinned change requests and Done, Printing (self by default, office still settable on the CRM side) / Mark ready for collection / Mark collected + auto-collect, product data auto-applying with Dismiss / Roll back while Designing or Changes Requested and the Keep / Update review from Design Ready on, History, changing a line's promotion or price from the Lines tab, the Lines rail keeping its scroll position and hidden on phones, which photo prints, readable spec values, the Price tag description merge field).
* ✅ Project Sales Admin - drafted (review PR, review Sponsorship Form).
* ✅ Project Sales Manager - drafted (approve via email, flow stock inquiry).
* ✅ Project Sales Rep - drafted (portal overview: verify card, centred dashboard with Filter / Sort / view toggle including the List DataGrid, duplicate; submit via portal, including the sectioned Price Tag Request form, per-file AI extract, no line reorder, packages and parts on a price tag line, and Revise on a submitted request instead of post-submit edit; review a price tag design: every request prints itself (no printing choice on the form), Design first + lightbox zoom/pan, pinned change requests and Send, Approve, Download PDF queuing its own export with Preparing your PDF / PDF failed, try again states, or Mark collected, WhatsApp at every status change).
* ✅ System Management - drafted (operational data reference, troubleshoot failed notifications, chatbot turn trace + retry + full trace panel (Apply, Memory, Sent, and the rest), chatbot photo and voice note intake (the "I read ..." / "I heard ..." reply prefix, naming a code it could not find, the entity cap, and each refusal wording) and the Chat History thread's photo/audio bubble, "Read N items" chip and "Read the photo" / "Heard the voice note" trace stage, chatbot "last purchase cost" answer + Field reveal gate, chatbot outstanding report (sales order backlog / delivery order pending) + its Field reveal gate, chatbot sales report (confirmed vs outstanding sales by month, breakdown by product or customer, the sales order list on "1", the current-year default on a product-only ask) + its own Field reveal gate, chatbot spec visibility policy (Show only / Hide these, contact override / market segment / default), chatbot "which products have X" set answers, Chatbot Domains (narrowing, ladder, prompt block, deferred delete) and Entity kinds, AutoCount integration contract, import column mappings including the per-supplier Stock list words vocabulary, the supplier chip and Ignored group on a saved mapping, and the inline column-mapping panel on Upload supplier documents and Plan a container (samples, header text, field picker, Ignore, the "N of N columns mapped from saved layout" fold and Review, the Header row stepper) that now writes those rows).
* ✅ Supply Chain - drafted (upload sales orders / purchase orders / order inquiry sheet / reorder levels; a re-upload of the order inquiry sheet rebuilding a row planning has since worked on (a used row, a row CS confirmed a different quantity or date for, a top-up), what it names instead of guessing when a match isn't exact, and what a rollback of that upload now keeps instead of removing; a Loading Plan stock-list upload composing our product code from a supplier's bare model number via the Stock list words vocabulary, merged 型号 cells filling through, and an unrecognised word waiting for a manual pick; sales & purchase history uploads retired in favour of the AutoCount integration; the Order Inquiries page opening on a Documents view by default (one row per order inquiry, oldest raised first, an All/Outstanding/Completed toggle, the Raised at/OI no/S/O no/Raised by/Lines/Qty/Customer/Project/Agent/SO date/Status columns, and search/filters reaching a line's own product or location), the monthly `OI-YYMM-NNNN` order inquiry number fixed for life with the old number still searchable, Raised at/Raised by fixed to the first raise with every later raise recorded on the OI detail page's Raise history, and the OI detail page itself (Lines/General/Related PO/Related SPO tabs, a pager over the list you came from, one Confirm acting on ticked lines or the whole order inquiry, and the gear menu's Auto link/Choose document/Link selected/Unlink selected/Reject selected/Unconfirm/Export Excel); the Lines toggle reaching today's worklist unchanged, its own List/Schedule toggle, the Buy/Purchased/Incoming stage cards, month tabs, Location/Agent/SO month/PO number/SPO number filters, the Excel-order column default, the via PO / via SPO / awaiting shipment tags, a row's PO/SPO following AutoCount's own linkage automatically (closed or received documents included), the Supplier column reading an SPO-only row's shipping-order supplier instead of Not linked, AutoCount's linkage displacing another sales order's row (with a note) and moving a link even after the goods arrived, and a row already fully linked being left alone until it is unlinked, the received / reallocate / unlink / note words on a chip and their lightboxes, a row already covered by stock (marked `used`, with the fresh row's own Was/Now and which document was received, and no separate delay row for that line), a row on a cancelled sales order line greyed and tagged `cancelled` (dropping out of Buy only, still counted in Purchased/Incoming when it holds a link), a used row or a cancelled-line row landing on To confirm once for purchasing to confirm as seen (state, quantity and links untouched), and the To confirm card itself toggling off on a second press like the other three; every row of one sales order sitting under one order inquiry number (the Order inquiry column hidden by default), cancelled rows hidden from the list by default, the Raised by column and filter naming the row's own raiser, the worklist's Customer and Project columns sorting independently and the PRE-ORDER note, the Raised at info icon and its Previously raised tooltip for a re-confirmed line, ticking every row and the Actions count labels, Unconfirm putting a Confirmed or Changed row back to To confirm, and the Schedule matrix; run a reorder plan; print the order summary; the low stock report workbook, its Field reveal gate, and splitting that workbook into sheets by Supplier, Category, or Supplier x Category from the same Actions dialog; Buy/borrow decisions on Fulfilment Planning, including the Local pill and the borrow-source Location table; planning an undecided sales order from the list, what counts as decided, the ordered-quantity ask and the Planned pill; sales order changes after planning, the board's composed suggestion and Confirm/Amend, the year on the What changed dialog's dates, and the `used` row Confirm raises when a linked document is already fully received; the order inquiry handover email to purchasing during the parallel run, its subject/headline/table shape, what sends and never sends it, and the Automation row that manages it; undo last confirm from the board's gear menu, the countdown, what it restores, the two refusal reasons and the automatic "Order inquiry undone" email to purchasing; the Fulfilment Planning Grid opening on By date by default (one column per line's own exact date, By day/week/month still offered) and the List view's leftmost Line column, its Sales order link landing on the exact line, and its OI column landing on that line's own order inquiry row; the Order Inquiries Lines tab and worklist's Taken and Remaining columns and their footers, the SO line / S/O line link to the exact sales order line, and a date move that settles onto the same row with a Changed tag and a Was/Now (i) instead of a second row; the State pill's plain words - To buy / Partly on PO/SPO / On PO/SPO / Done / Cancelled - on the Lines tab, the worklist and the board alike; the admin fold_oi_date_notices.py script for folding old duplicate date-move notices; asking CS to reserve stock against an order inquiry line (the Lines tab's amber/green Reserve icon, the Request CS to reserve dialog defaulting Requested to the remaining and Location to the configured pool, Cancel request's countdown, and requesting again on the balance), CS reserving against a request (the same dialog's Reserve tab, its Location/Reserved/Reason fields and Confirm reserved, the row's own History tab, and Unreserve releasing the newest reserve first with no email), the reserve request and reserved emails and the Automation rows and recipient checkboxes that manage them, the Reserve dialog default location setting, and the Reserve Stock for Order Inquiries permission); Plan a container's inline column-mapping panel for a stock list or proforma invoice file (samples, header text, field picker, Ignore, the required-field gate on Test, the Header row stepper, and the saved-layout fold that only reopens on an unseen column).
* ✅ User Management - drafted (Configure Portal Revisions, naming the Price Tag Request row, Chatbot settings (Switches, Memory, Tier order, Cross-domain ladder), the per-contact Chatbot card (Stock checks, Episode recall, Tier, Language); data reference now covers Market Segments' Additional portal forms grant and the contact record's Market segment / Portal forms blocks).

## Source-of-truth notes

* **Auto-link is the default.** Generic file uploads (Pattern A) fire an outbound webhook to **n8n** with the file URL and the attachment-type name. The integration workflow attached to that type parses the file, creates the relevant business records, and **calls back into the CRM** to attach the file to those records. Users do **not** link files manually after upload.
* The columns the parser expects (e.g. for a packing-list Excel) are defined in the n8n workflow attached to that attachment type, **not** in this codebase. Ask your integrations admin for the current template if a parser fails.
* The only attachment types seeded by Alembic migrations are **Promotion** (`code = 'promotion'`) and **Complaint Document** (`code = 'complaint_document'`). All other types (*Packing List*, *Product Attachments*, *Marketing Form*, etc.) are tenant-specific and must be created under **[Resource Management → Attachment Types](/resource-management/attachment-types)**.
* Module-specific imports (Pattern B) - `product_import`, `spo_import`, `grn_listing_import`, `grn_lines_import`, `stock_import`, `order_tracking_import`, `delivery_order_detail_import` - are processed by the backend's import-job pipeline and surface progress via the `Latest … import` panel on the relevant module's list page.
* **Approval links are token-based, one-time use, and expire after 24 hours.** Project sales managers approve / reject purchase requests and sponsorship forms via the emailed link without logging in to the CRM. If a link expires, the project sales admin can re-issue one from the request detail page.
* **Project sales reps are notified only via WhatsApp** through Respond.io - there is no in-portal notification inbox. WhatsApp covers: complaint reply, stock-inquiry decisions (flowed / rejected / responded), and PR / sponsorship-form approval / rejection.
* **AI Extract on the portal is server-side.** When a rep clicks **AI Extract** on a complaint, the file is uploaded to the backend and an LLM proposes field values; the rep confirms before they're applied. Currently enabled on **complaint** submissions only.
