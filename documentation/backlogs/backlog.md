# Backlog Register

Deferred work, with linkage back to the plan that spawned it. Add a row when a plan consciously
defers something (hardening, follow-up, migration, prod backfill); close it when a later plan
picks it up. One register, single source of truth - do not scatter TODOs across plan docs.

Format: `ID · Title · Source plan · Priority · Status`. IDs are `BL-<NNN>`, monotonic.

| ID | Title | Source plan | Priority | Status |
|----|-------|-------------|----------|--------|
| BL-001 | **Run the import-tracking master-ref upsert backfill in prod** - the DO-import path fix (customer/transporter FK upsert) is merged but the idempotent JOIN-based backfill script has only run against the local prod-copy DB, not real prod. | `plans/PLAN-import-tracking-master-ref-upsert-backfill.md` | High | Open |
| BL-002 | **Security-cluster Phase 2 (authz / B)** - Phase 1 (bounded bugs C1 - C4 + rate-limit A) landed on `fix/phase1-bounded-bugs`, unmerged. Phase 2 = the authz hardening (B), blocked on verifying the n8n `EXTERNAL_API_KEY_ACT_AS_USER_ID` act-as role has exactly the grants it needs. | `plans/PLAN-fix-security-cluster.md` | High | Open |
| BL-003 | **Form-SLA duplicate `sla_assigned` WhatsApp** - double send traced to `_active_tracker` ignoring `team_set_code` + stages sharing one `policy_id`. Fix pending a grill on the correct scoping. | `plans/PLAN-form-sla-dup-shared-policy.md` (to author) | Medium | Open |
| BL-004 | **Enqueue the flyer read instead of holding the request** - the read is off the event loop but is still 15 to 60 s of foreground work with no progress feedback. When artwork rasterisation lands this becomes an enqueue returning 202 with a row to watch, as the catalogue PDF export already does. Picked up early: production 504'd on the real flyer at the gateway (2026-08-15), so the read is now an RQ job on the `flyer_read` queue with the reading row carrying `processing` / `done` / `failed`. | `plans/dealer-kit/PLAN-flyer-read-background-job.md` | Medium | Done |
| BL-005 | **Collapse the two forked link-attachment browsers** - the shared picker now lives at `components/common/LinkAttachmentBrowserDialog.tsx` and is used by five modules, but near-identical forks remain under `master-data-management/products` and `marketing-management/promotions`. | `plans/dealer-kit/PLAN-flyer-read-hardening.md` | Low | Open |
| BL-006 | **A flyer whose mime was lost on import is unpickable** - the from-attachment route accepts NULL / `application/octet-stream`, but the picker filters on the six positive PDF spellings so those rows are never offered. Closing it needs a filename or extension filter on the attachments list, not a wider mime filter, which would list every binary blob in the library. | `plans/dealer-kit/PLAN-flyer-read-hardening.md` | Low | Open |
| BL-007 | **The library flyer read has no committed regression guard** - AC-A10 is met by a reproducible agent-browser evidence run only, because the branch's new Playwright spec was dropped under the standing order that no project carries a playwright trace. Give it a guard when the repo-wide replacement for the e2e specs is decided; the same decision covers the existing dealer-kit specs driving clicks through `dispatchEvent`, which bypasses actionability checks. | `plans/dealer-kit/PLAN-flyer-read-hardening.md` | Medium | Open |
| BL-008 | **Frontend tree is not Prettier-clean** - `sorento_crm_frontend/.prettierrc` now pins the measured prevailing style (single quotes, semis, printWidth 80, trailingComma all, no plugins), but `npm run format:check` still flags 1743 of ~2900 files, so no CI format gate exists. Closing it is a dedicated repo-wide `npm run format` PR (nothing else in it), then a `format:check` step in `.github/workflows/deploy.yml`. Also decide whether the three installed but inert prettier plugins (`@ianvs/prettier-plugin-sort-imports`, `prettier-plugin-organize-imports`, `prettier-plugin-tailwindcss`) are dropped or enabled in that same PR. | (none - tooling chore, PR "chore(frontend): add a Prettier config") | Low | Open |
| BL-009 | **Take the remaining heavy `async def` handlers off the event loop** - a sweep found about 40 handlers doing sync storage uploads / PIL thumbnails, inline openpyxl imports (`maybe_strip`, `validate_*_import`, `store_import_source_file`, `scm/upload_intake.read_upload`), sync `RespondClient` HTTP, and the module zip install (`alembic upgrade` in-request) ON the loop. Same class as PR #164 and the portal ai-extract fix. Fix at the shared helpers plus a per-router AC-J3 style metadata test. Full list in the source plan. | `plans/ai-extract/PLAN-ai-extract-off-the-loop.md` | High | Open |
| BL-010 | **A flyer read whose worker dies mid-job stays `processing` forever** - the `flyer_read` job marks its row `done` or `failed` itself, so an RQ work-horse killed by the job timeout, an OOM or a worker restart leaves the row in `processing` with nothing to flip it. Needs a sweeper (scheduler tick: rows `processing` older than the job timeout with an RQ job that is not queued/started -> `failed` "The read was interrupted") or an RQ failure callback. Same gap exists for `downloads`. | `plans/dealer-kit/PLAN-flyer-read-background-job.md` | Low | Open |
| BL-011 | **Opening a flyer report takes seconds on a busy box** - `GET /flyer-readings/{id}` recomputes the match over 998 codes on every read (0.9 s quiet, measured 6.3 s loaded on 2026-08-16). Deliberate, since a stored report goes stale invisibly, but the fix when it bites is a cache invalidated on product and promotion writes, not a column. | `plans/dealer-kit/PLAN-flyer-read-background-job.md` | Low | Open |
| BL-012 | **The spec extraction flow has no committed regression guard** - AC-B.17 is met by a reproducible agent-browser evidence run only (recorded in the PLAN, "PR 4 evidence run" section), under the standing order that no project carries a new Playwright spec. Give the paste-extract-apply flow a guard when the repo-wide e2e replacement is decided. | `plans/master-data/PLAN-spec-authoring-verification.md` | Medium | Open |
| BL-013 | **Decide the fate of `test-backend-xdist-shadow`** - the shadow CI job runs the FULL gated backend suite under `pytest-xdist -n auto` on every push and PR, gating nothing, purely to gather flake evidence on order dependence before the real gate is trusted with parallelism. It costs a runner every run, so it must not live forever: by ~2026-08-31 either cut `test-backend` (and `test-backend-scm`) over to `-n auto` and delete the shadow, or delete the shadow and record why parallelism was rejected. Decided, cutover done 2026-08-17: the shadow was promoted to the blocking `test-backend` gate (name "Backend test suite (Postgres, xdist)") running `-n auto --dist loadfile`, and the serial job was deleted. `--dist loadfile` was the CONDITION of the cutover, not a tuning choice: about 38 test files use a per-file marker plus an autouse wipe of rows carrying it plus real commits on the shared database, a pattern that assumes the tests of one file run serially. The proof was five UniqueViolation failures on `respond_contacts_phone_number_key` from `tests/test_conversation_sla_neighbours.py` under the default `--dist load`, gone under `--dist loadfile`. | `.github/workflows/deploy.yml` (CI speedups, no plan doc) | Medium | Done |
| BL-014 | **The brand-routing flow has no committed browser regression guard** - AC-V2 is met by a reproducible agent-browser evidence run only, since no new Playwright spec is added under the standing order that no project carries a playwright trace. Give it a guard when the repo-wide replacement for the e2e specs is decided (same decision as BL-007). | `plans/sla/PLAN-brand-aware-routing.md` | Medium | Open |
| BL-015 | **`tests/test_market_segment_routing.py` is red on main** - it predates the `/external` permission gate and calls the roster / next-assignee routes without granting them, so every case 403s. Fix is to wrap the calls in `tests/_external_auth.external_permissions_granted`, the helper the other external suites already use. Not caused by brand routing; found while running that suite. | `plans/sla/PLAN-brand-aware-routing.md` | Medium | Open |
| BL-016 | **`tracker.brand_code` is not refreshed by a reassign, by design** - settled by the member-level revision: the brand describes the ENQUIRY (what the customer asked about), not who is handling it, so a takeover, a manual reassign and `apply_assignee_team_derivation` all leave it exactly as stamped at creation. The revision-1 re-stamp (which read the brand off the `AgentTeam` row landed on) is gone with that column. Pinned by `test_tracker_brand_escalation.py::test_a_manual_reassign_leaves_the_brand_alone`. | `plans/sla/PLAN-brand-aware-routing.md` | Low | Done |
| BL-017 | **Deleted the dead `AccessAgentForm.tsx` (+ its test)** - the detail-page form was superseded by `AccessAgentFormModal` and had no importer outside its own test; removed in the brand-routing PR (which then had one team-assignment editor, not two). Still correct after the member-level revision. | `plans/sla/PLAN-brand-aware-routing.md` | Low | Done |
| BL-018 | **The brand picker on the access-agent pages needs `master_data.brands.view`** - `MemberBrandEditor` is fed by `useBrandSelectQuery` -> `GET /api/master-data/brands/select`, which is permission-gated separately from `access_agents.edit`. A user who may edit routing but cannot read brands gets an error toast and an empty dropdown, with no way to tell that the routing page itself is fine. Either co-grant the read with `access_agents.edit` or serve the options from a routing-scoped endpoint. Same endpoint also did `limit(100)` with no `order_by`, so a tenant past 100 brands got an arbitrary, unstable 100 - the `order_by(Brand.brand_name)` half landed with BL-019; the permission half is what keeps this row open. | `plans/sla/PLAN-brand-aware-routing.md` | Medium | Open |
| BL-019 | **The two member editors are hand-rolled checkbox popovers, not `SearchableMultiSelect`** - `MemberBrandEditor` (new) and `MemberMarketSegmentEditor` (its parity source, pre-existing) both rendered a `Popover` + `Checkbox` list, which is the non-searchable-dropdown shape the standards forbid. BOTH migrated together: each is now a `Dialog` holding a `SearchableMultiSelect` (dialog, not popover, because `SearchableMultiSelect` opens its own portalled popover and Radix reads that as an outside click on a parent popover - `DialogContent` already guards that case). Brand options are deduped per company and sorted A to Z, and `GET /master-data/brands/select` gained `order_by(Brand.brand_name)`. Second half closed too: a tag whose brand/segment has left the catalogue is still offered as an option and Save is no longer disabled on an empty catalogue, so a stale tag can be cleared back to "serves all". | `plans/sla/PLAN-brand-aware-routing.md` | Medium | Done |
| BL-020 | **The spec verification worklist flow has no committed regression guard** - AC-D.19 is met by a reproducible agent-browser evidence run only (`plans/master-data/spec-authoring-verification-evidence-pr3.md`), under the same standing order as BL-012. Give the worklist verify/unverify round-trip a guard when the repo-wide e2e replacement is decided. | `plans/master-data/PLAN-spec-authoring-verification.md` | Medium | Open |
| BL-021 | **Three sibling LLM call sites still read the generic key column** - `ticket_intake_service`, `ideation_extractor` and `scm/explainer_service` read `api_key_ciphertext or settings.openai_api_key` instead of `resolve_api_key(config, provider)`, so an assistant configured on Gemini or Anthropic with the key entered only in the provider's own field posts the wrong vendor's key. The media, ai_extract, product_spec, voice and the three assistant chat paths are converted; these three were out of scope for the media PR and are left as-is. Same one-line conversion each, plus a behaviour test that the resolved key reaches the provider. | `plans/ideation/PLAN-chatbot-media-endpoint.md` (review follow-up, PR #158) | Medium | Open |
| BL-022 | **Captain decision: pin `media_url` / `callback_url` to known hosts?** - both are caller-supplied on `POST /external/media/process` and are fetched / POSTed verbatim by the worker (httpx, `follow_redirects=True`), so a holder of the external key plus `integration.chatbot_media.process` can reach internal hosts through the worker and read the fetch result via the extraction output. Mitigated today by the authenticated-integration trust model (only n8n holds the key). Deliberately NOT fixed in PR #158; the question for the captain is whether to allow-list the Respond.io CDN host for `media_url` and the n8n host for `callback_url`, which costs little but couples the CRM to those hostnames. | `plans/ideation/PLAN-chatbot-media-endpoint.md` (review-10, PR #158) | Low | Open |
| BL-023 | **The Stage 1B Fulfilment Planning flow (list -> side sheet -> Re-run reconciliation) has no committed browser regression guard** - AC-A01 / AC-A03 are met by the recorded agent-browser evidence runs only, since no new Playwright spec is added under the standing order; the live run also could not open the sheet because every local Project SO is blocked (nothing published), so that half rests on `tests/test_project_so_reconciliation_routes.py`. Re-walk it, or add the guard once the fleet decides what replaces the specs. | `plans/scm/STAGE1B-scm-front-planning-reconciliation.md` | Medium | Open |
| BL-024 | **The per-location plan grid rounds channel needs to whole units** - `PlanLinesGrid.tsx` renders every channel figure through `fmtInt` (the `ChannelNeed` cell, and `numCell` for the shared supply columns), so a `kg` product needing 2.5 reads as "3" in the per-location grid while the Summary Order Report prints the same product as 2.5 at its frozen `uom_decimal_places` (AC-F12). The two screens disagree about the same number. Not patched here because the fix is a contract addition, not a formatter swap: the recommendation payload carries no precision, so `GET /scm/reorder-runs/{id}/recommendations` must first carry the run's frozen `uom_decimal_places` per row, the way the summary rows already do. Do it with that contract change, and format both grids off the frozen snapshot rather than live UOM master data. | `plans/scm/PLAN-scm-front-planning.md` | Medium | Open |
| BL-025 | **Editing a packing list through its ordinary form wipes the captured unit cost and currency** - `procurement_service.update_shipment` rebuilds every line from the payload, and the FE form's line schema carries only `product_id` and `quantity_shipped`, so any save (a note, a date) reinserts the lines with `unit_cost` and `currency` NULL. The supplier then reads as "never received" on the Order Decision sheet and the PI-vs-PO check loses its incoming side. The mechanism pre-dates the proforma slice; what changed is that the ingest now fills those columns, so the wipe went from theoretical to the normal outcome of a routine edit. Deliberately not fixed there because `update_shipment` and that form belong to the packing-list task: preserve both fields from the replaced line matched on `product_id` when the payload states neither, or carry them through the form's line schema. | `plans/scm/scm-proforma-invoice-acceptance-criteria.md` (Known limits) | Medium | Open |
| BL-026 | **The multi-supplier container flow has no committed browser regression guard** - the data-loss fix (a second supplier's packing list accumulates instead of replacing the first's) and the consolidated Sorento packing-list panel on `/scm/incoming` are pinned by pytest (`tests/test_packing_list_multi_supplier.py`, `tests/test_consolidated_packing_list.py`) and vitest (`scm/incoming`, 41 tests) only; no agent-browser evidence run was recorded because the lane had no test-stack slot, and no new Playwright spec is added under the standing order. Give the upload-two-suppliers -> panel shows both factories -> re-upload one flow a guard when the repo-wide e2e replacement is decided (same decision as BL-007). | `plans/scm/PLAN-scm-container-multi-supplier.md` | Medium | Open |
| BL-028 | **The product-discontinued scope editor has no committed browser regression guard** - AC-19 is met by a reproducible agent-browser evidence run only (recorded in the PLAN), since no new Playwright spec is added under the standing order that no project carries a playwright trace. Give the edit-scopes-save-reopen round-trip and the brand-narrowed products deep link a guard when the repo-wide e2e replacement is decided (same decision as BL-007). | `plans/PLAN-product-discontinued-brand-scope.md` | Medium | Open |
| BL-029 | **Write-side company gating on `replace_scopes` is missing** - saving a user's discontinued scopes only checks that the named company/brand exist and agree, not that the acting admin may reach that company; the read side (`GET /brands/select?company_id=`) already refuses one they cannot. Deferred because it needs a decision on which permission governs it (`user_management.users.edit` is not company-scoped). Rationale in the UAC's Follow-ups section. | `plans/product-discontinued-brand-scope-acceptance-criteria.md` | Medium | Open |
| BL-027 | **Seven `/scm` tables show a sort affordance and never sort** - `reorder/components/{PoWorklistView,PlanExceptionsView,NeedsLevelView,CoveredByStockView}.tsx`, `simulation/components/ScenariosGrid.tsx`, `loading-plan/components/LoadingPlanView.tsx` and `incoming/components/ConsolidatedPackingListPanel.tsx` build `useReactTable` with `DataGridColumnHeader` headers but no `getSortedRowModel` (and no `manualSorting`), so the arrow flips and the rows stay put. Same defect the captain hit on the SO detail lines (fixed on `SalesOrderDetail.tsx` + `PurchaseOrderDetail.tsx`, 18 Aug 2026). Fix each with `SortingState` + `getSortedRowModel()` and numeric / ISO-date `accessorFn`s on quantity and date columns; several carry id-only computed columns that need an `accessorFn` the way "Still owed" did. | `plans/scm/PLAN-fulfilment-planning-from-autocount-so.md` (13.10 live amendments 18 Aug) | Medium | Open |
| BL-030 | **`pool_claims` and `_pile_book` tie-break differently** - the pool queue breaks a score tie on `(so_number, line_no, id)` while the pile book breaks it on `(required_date, so_number, ...)`, so under a policy that scores dates at 0 the pool's own queue is alphabetical by SO number rather than by date. The live fair policy weights the date, so the two agree today; align the tie-break (one helper both call) so the trail's rung 2 explanation can never contradict the queue dialog. Found by coder J2 on 19 Aug 2026 while wiring the pool pile into the trail. Closed 19 Aug 2026 in the code-review pass on that plan: `_pile_book`'s scoring and tie-break now live in one helper (`ProjectSupplyService._rank_pile` over `_pile_order`) and `pool_claims` inserts the asking line into the pool's queue and ranks the lot with it, pinned by `test_a_pool_draw_is_queued_by_the_same_rule_the_pile_book_orders_the_pool_by`. | `plans/scm/PLAN-fulfilment-planning-from-autocount-so.md` (13.11 / 13.12) | Low | Done |
| BL-031 | **`tests/test_product_predicate_service.py` is red between 00:00 and 08:00 Malaysia time** - `test_certificate_object_form_filters_on_validity` and `test_promotion_leg_requires_an_active_unexpired_promotion` seed `valid_until` / `end_date` with the local `date.today() - 1 day` while the service compares against the UTC date, so for eight hours a day 'yesterday' is still 'today' and the expired row is kept. Green under `TZ=UTC` (CI). Pre-existing on main; fix by seeding relative to the same clock the service reads (or by using a two-day margin). | `plans/scm/PLAN-fulfilment-planning-from-autocount-so.md` (found during the 19 Aug empty-DB proof run) | Low | Done - fixed 2026-08-21 (deflake sweep): seeds now use `datetime.now(timezone.utc).date()` (`_utc_today()`) instead of local `date.today()`, matching the `func.current_date()` clock the service reads on a session pinned to UTC. |

> Seeded 2026-07-13 from in-flight work at the time of the documentation restructure. Historical
> plans predating this register carry their own inline "Deferred / follow-ups" sections; migrate
> those into rows here as each is next touched (going-forward, not a bulk sweep).

- **BL-032** (2026-08-19): `ReorderLevelUploadDialog.test.tsx` "applies on confirm and reports what actually happened" flakes in CI - asserts the Confirm button is gone while it is still in its loading state (spinner). Passes locally. Fix: `waitFor` the mutation to settle before the not-in-document assertion. Cost one rerun on PR #224. **Status: Done** - fixed 2026-08-21 (deflake sweep): the not-in-document assertion is now inside its own `waitFor`.
- **BL-033** (2026-08-19): `scm/incoming/components/PackingListUploadDialog.test.tsx` "carries the currency ..." flaked in CI on PR #228 (`expected last vi.fn() call to have been called with [File, ...]`) on a run whose branch had not touched the file; passed locally (16/16) and on the previous CI run of the same code. Same family as BL-032 (assertion before the async handler settles). Fix: `waitFor` the upload mock before asserting its last call. Cost one rerun. **Status: Done** - fixed 2026-08-21 (deflake sweep): the `applyPackingList` validate-only assertion is now inside a `waitFor`.
- **BL-034** (2026-08-19): full backend xdist suite flaked once each on two consecutive PR #228 runs with files the branch never touched - `tests/test_complaint_neighbours.py::test_neighbours_out_of_filter_falls_back_to_unfiltered` (`assert 4 == 8`) and `tests/test_idempotency.py::test_duplicate_within_window_runs_once` (`409 == 200` expected); both green locally and on the rerun. Shape is cross-worker shared state (another worker's rows / idempotency key). Fix: marker-scope both tests' fixtures and assertions so a neighbour's rows cannot change their counts. Cost one rerun each. Recurred 2026-08-21 on main's post-merge run (`assert 4 == 6`, same test). **Status: Done** - fixed 2026-08-21. `test_complaint_neighbours.py`: dropped the equality check between two separately-timed live reads of the whole shared `complaints` table (the actual race - another worker's file can insert/delete rows in the gap between them) and replaced it with a check that this test's OWN marker rows are all present in the fallback's unfiltered scope, which needs no ambient row count. `test_idempotency.py`: root cause was `tests/conftest.py`'s `_reset_global_state` autouse fixture, which flushes every `idemp:*` key on the DEFAULT Redis db after EVERY test in the suite with no per-test scoping - a different xdist worker's unrelated test can fire it in the gap between this test's own r1 and r2 calls and delete r1's cached "done" entry before r2 reads it, so r2's replay wait times out and returns 409. Moved this file onto its own Redis logical db (`_ISOLATED_REDIS_DB`, fixed index, not derived from anything random) so the global flush's cached client - pinned to the default db - can never reach it, without narrowing what that fixture protects for every other test. Recurred again the same day, one file at a time, as CI worked through the rest of the family: `test_conversation_sla_neighbours.py` (CI run 96606018441 - the out-of-filter total AND wrap/sort-dir/excludes-form-sla tests, the latter fixed by passing `tracking_ids=[own ids]`, an id-list scope `neighbours()` already supports). Swept every other live-DB `*_neighbours` file sharing the same "out of filter falls back" template and fixed each the same way (membership proof against the D2 fallback's own query, never a separately-fetched total): `test_access_agent_neighbours`, `test_attachment_neighbours`, `test_customer_neighbours`, `test_form_neighbours`, `test_grn_neighbours`, `test_order_neighbours`, `test_packing_list_neighbours`, `test_product_neighbours`, `test_purchase_request_neighbours` (scoped to request_type), `test_supplier_neighbours`. `test_promotion_neighbours` / `test_portal_submission_neighbours_ownership` use the isolated `blank_session` scratch schema and were already immune; `test_stock_inquiry_neighbours` was left to a second coder working the same sweep concurrently.
- **BL-035** (2026-08-20): Reorder Planning console logs `[Table] Column with id 'warehouse' does not exist` x4 under the product-grain grouped view - the user's persisted column preferences (list-query column-config for /scm/reorder) still name the Location column that grouped mode no longer registers. Harmless (grid renders), but a real console error. Fix: the DataGrid should drop unknown ids from saved prefs instead of erroring, or the grouped view keeps the column id registered hidden. Found by the 20 Aug evidence run.
- **BL-036** (2026-08-20): `suppliers.supplier_code` still carries a bare column-level `unique=True` on the model while production carries the per-company `uq_suppliers_company_supplier_code` (migration 305), so CI's `create_all` schema is stricter than production's. `tests/scm/test_outstanding_import_company_isolation.py` answers this with a probe-and-skip, which means `test_a_creditor_code_owned_only_by_another_company_gets_its_own_row_back_created` never runs in CI - the one place the assertion matters. Fix the divergence at the model (`__table_args__` unique on `(company_id, supplier_code)`, drop the column-level flag), then delete the skip. Every future create_all-schema test inherits the wrong constraint until this is done. Found by the 20 Aug review of PR #231.
- **BL-037** (2026-08-21): `components/common/conversation/InternalCommentComposer.test.tsx` "typing @ opens the typeahead over the SHARED user select service" flaked in CI on PR #231 (run 32392177995) - `expected vi.fn() to be called with ObjectContaining {"query": "Te"}` - on a branch that never touched the file; 17/17 green locally and the file's last commit is from July. Same async-assertion-before-settle family as BL-032/033: fix with a waitFor around the typeahead-query assertion. Cost one rerun.
- **BL-038** (2026-08-21): `tests/test_project_samples.py::test_a_version_from_another_project_is_refused` flakes at ~0.3% per run: the fixture titles projects `zzt-sample Tower <uuid hex[:6]>`, and when the random suffix carries no digit (all hex letters, ~(6/16)^6), `are_sibling_developments`' phase-designator regex yields an empty set, the sibling exemption falls through, and the shared title template's trigram similarity (~0.708) clears the 0.70 block bar - the second seeded project 409s before the assertion under test. Fix in the fixture (force a digit into the suffix) or in `phase_designators` (handle the empty-set comparison). Diagnosed 21 Aug during PR #231's xdist run; not the BL-034 family. **Status: Done** - fixed 2026-08-21 by replacing the random hex suffix with a deterministic, always-digit-bearing counter (`_title_suffix()`), so the designator set can never come up empty. Same disease, second sighting, same day: `tests/test_order_inquiry_unplace_all.py::test_another_companys_placed_row_is_neither_previewed_nor_touched` killed main's deploy attempt 3 with a 409 `project_already_registered` - `_seed_world`'s two projects ("Project One"/"Project Two", sharing a 19-char marker prefix + "Project ") sat close enough to the 0.70 trigram block bar that the random suffix draw occasionally pushed the second registration's score over it against the first, company-scoped so it was the test's own two seeds colliding, not another worker. Fixed by giving the two projects genuinely dissimilar titles (Alpha Quay / Zeta Ridge) AND pinning `block_threshold=2.0` (above the maximum possible trigram score of 1.0) on both `register_project` calls, so the block path cannot fire regardless of the suffix draw or a concurrent worker's `system_settings` threshold edit - deterministic, no retry semantics. One edit in the shared `_seed_world` covers both the main-company and other-company worlds the file seeds.
- **BL-039** (2026-08-21): `components/common/conversation/InternalCommentComposer.test.tsx` "typing @ opens the typeahead over the SHARED user select service" flaked in CI on PR #231 (run 32392177995) - `expected vi.fn() to be called with ObjectContaining {"query": "Te"}` - on a branch that never touched the file; 17/17 green locally and the file's last commit is from July. Same async-assertion-before-settle family as BL-032/033: fix with a waitFor around the typeahead-query assertion. Cost one rerun. **Status: Done** - fixed 2026-08-21 (deflake sweep): the `getUsersSelect` query assertion is now inside a `waitFor`.
- **BL-040** (2026-08-21): `tests/test_project_samples.py::test_a_version_from_another_project_is_refused` flakes at ~0.3% per run: the fixture titles projects `zzt-sample Tower <uuid hex[:6]>`, and when the random suffix carries no digit (all hex letters, ~(6/16)^6), `are_sibling_developments`' phase-designator regex yields an empty set, the sibling exemption falls through, and the shared title template's trigram similarity (~0.708) clears the 0.70 block bar - the second seeded project 409s before the assertion under test. Fix in the fixture (force a digit into the suffix) or in `phase_designators` (handle the empty-set comparison). Diagnosed 21 Aug during PR #231's xdist run; not the BL-034 family. **Status: Done** - fixed 2026-08-21 by replacing the random hex suffix with a deterministic, always-digit-bearing counter (`_title_suffix()`), so the designator set can never come up empty. Recurred same day in a second file with the same fixture shape: `tests/test_order_inquiry_unplace_all.py::test_another_companys_placed_row_is_neither_previewed_nor_touched` (killed main deploy run 32405398212 attempt 3) - fix being carried separately.
- **BL-041** (2026-08-21, deflake sweep): the BL-034 shape (`test_complaint_neighbours.py::test_neighbours_out_of_filter_falls_back_to_unfiltered` comparing two separately-fetched live totals of a real shared table for equality) was not unique to complaints - the same `_out_of_filter_falls_back_to_unfiltered` test existed, unfixed, in 13 more `test_*_neighbours.py` files (attachment, access_agent, conversation_sla, customer, form, grn, order, packing_list, product, promotion, purchase_request, stock_inquiry, supplier), all seeding real committed rows via a bare `SessionLocal()` with no rollback-only isolation. **Status: Done** - fixed 2026-08-21: each rewritten to the same pattern as the (separately fixed) `test_complaint_neighbours.py` - assert the fallback total is merely bigger than the filtered subset, then prove the fallback stayed unfiltered by checking this test's OWN marker rows are a subset of the ids the service's own D2 branch reads, instead of comparing two racy live totals. Two concurrent coders swept the family independently; PR #234 (merged first) carried 11 of the files, the sweep PR carries the remaining two (`test_promotion_neighbours`, `test_stock_inquiry_neighbours`) plus this register. `test_purchase_request_neighbours.py`'s sibling (`..._falls_back_scoped_to_request_type`) also asserts the sponsorship-form row is excluded from the request_type-scoped fallback.
- **BL-042** (2026-08-21, deflake sweep): `db.query(ProductCategory.id).first()` (and any other first query against a `CompanyScopedMixin` table) on a freshly-opened `SessionLocal()` returns 0 rows even though matching data exists, because `tests/conftest.py`'s `after_begin` listener (which defaults an unset company scope to Sorento for the test process) fires AFTER `do_orm_execute` has already built the SELECT's company predicate off the still-unset scope - the scope is set in `session.info` in time for the NEXT query, not the one that triggered the autobegin. `tests/test_product_neighbours.py::_fk_ids` hits this on literally the first operation of a fresh session (a bare FK lookup before any insert warms the session), so every test in that file fails locally against a real dev DB that has categories/UoMs (confirmed pre-existing on `main`, unrelated to any of the neighbours-race fixes above - warming the session with one throwaway query before `_fk_ids` makes the whole file pass). Not fixed here: the fix is in shared `conftest.py` fixture plumbing, not a single test file, and is a fourth flake shape distinct from the RNG/wall-clock/xdist-count trio this sweep targeted.
- **BL-043** (2026-08-21, PR #237): the outstanding PO upload's PREVIEW does not show which CRM-raised POs the apply would retire - `apply()` supersedes matching active `scm_recommendation` POs (captain ruling a, closes line + PO with the AutoCount number on record) but `preview()` was not given parity, so the buyer first learns of the retirement from the post-apply summary's `superseded_documents`. Add the would-be-superseded set to the preview payload + dialog.
- **BL-044** (2026-08-21, PR #237): the order-inquiry auto-place "import" trigger is still unwired - `auto_place_for_products` documents three triggers but only `po_confirm`, decision confirm and the worklist button exist; after an outstanding import supersedes a CRM PO, the freed rows sit `raised` until a human hits the worklist button or a later PO confirm fires. Wire the import path to call auto-place (trigger="import") after apply commits, best-effort.
- **BL-045** (2026-09-01, S2 of `plans/scm/PLAN-scm-reorder-oi-feedback-1sep.md`, ruling G2): dead-stock and overstock disposition rows no longer surface anywhere - the reorder run stopped emitting `disposition` recs (the run universe is committed-demand only, so "dead" and "in this plan" are now a contradiction in terms), but `reorder_engine.disposition()` and the per-cell/per-location classification (`cell["disposition"]`, `_disposition_label`, `_transfer_flags_for`) are all still computed and simply thrown away. A dead-stock / overstock report needs its own surface (list + counts, likely off `reorder_engine.disposition()` run outside the reorder plan, or a dedicated background job) - not a reorder-plan recommendation type.
